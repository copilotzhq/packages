This directory is managed by Changesets.

Create a release note file with:

```bash
npm run changeset
```

Then commit the generated markdown file together with the package changes you want to release.
